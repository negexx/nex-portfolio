# ML artifacts — we audit this pattern; don't commit
*.pkl
*.h5
*.keras
*.pt
*.pth
*.onnx
*.safetensors
!tests/fixtures/**

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage

# Env
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Notebook artifacts (re-runs only)
.ipynb_checkpoints/

# Project runtime
mlsecops-agent/runs/
mlsecops-agent/reports/
*.sqlite
*.sqlite-journal

# ML artifacts (we audit this pattern — don't commit)
*.pkl
*.h5
*.pt
*.pth
*.onnx
*.safetensors
!tests/fixtures/**/*.pkl
!tests/fixtures/**/*.h5

# Claude per-developer state
mlsecops-agent/.claude/settings.local.json
mlsecops-agent/.claude/memory/
!mlsecops-agent/.claude/memory/README.md

# Local v2 execution scratch (TF venv + NSL-KDD dataset + executed notebook).
# The executed notebook is copied up to the repo root once the run finishes.
.v2_run/
